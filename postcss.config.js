// postcss.config.js
module.exports = {
    plugins: [
      require('tailwindcss'), // Ensure Tailwind CSS is properly required
      require('autoprefixer'), // Ensure Autoprefixer is properly required
    ],
  };
  